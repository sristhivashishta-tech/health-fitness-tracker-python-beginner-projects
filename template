<!DOCTYPE html>
<html>
<head>
    <title>Health Fitness Tracker</title>
</head>
<body>
    <h2>Health & Fitness Tracker</h2>

    <form method="POST">
        <input type="number" step="0.1" name="weight" placeholder="Weight (kg)" required><br><br>
        <input type="number" step="0.01" name="height" placeholder="Height (meters)" required><br><br>
        <button type="submit">Check</button>
    </form>

    {% if result %}
        <h3>{{ result }}</h3>
    {% endif %}
</body>
</html>